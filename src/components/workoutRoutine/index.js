import { Checkbox } from "@mui/material"

export const WorkoutRoutine = (props) => {
    return(
        <div className = "mt-4 flex flex-row">
            <Checkbox sx = {{padding:"0px"}}/>
        </div>
    )
}